# Test and coverage tooling. Kept separate from requirements.txt because the
# Lambda deployment package must not carry it - every megabyte in the bundle
# is cold-start time, and pytest has no business in a running function.
#
#   pip install -r requirements.txt -r requirements-dev.txt
pytest>=8.0
pytest-cov>=5.0
